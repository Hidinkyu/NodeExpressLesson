// CommonJS, every file is module (by default)
// Modules - Encapsulated Code (only share minimum)
const names = require('./3.1-nameModu')
const sayHi = require('./3.2-utilsModu')
const data = require('./3.1.1-alternative')
require('./3.3-mindgrr')

//   console.log(data);

// sayHi('susan')
// sayHi(names.john)
// sayHi(names.jane)
// sayHi(names.peter)